import styled from 'styled-components';
import { space, layout, flexbox, color, background } from 'styled-system';

export const Root = styled.div`
  display: flex;
  ${space}
  ${layout}
  ${color}
  ${background}
  ${flexbox}
`;
